#include <stdio.h>
#include <stdlib.h>

SixSteps {

  event complete(void){}

  event step(void){}

  ere : step^7

  @match {
    printf("too many steps!\n");
    exit(1);
  }

  @fail {
    __RESET;
  }
}
