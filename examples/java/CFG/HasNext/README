The java and javac commands need rvmonitorrt.jar and the monitor directory on your CLASSPATH.

    javac -cp [rvmonitorrt]:[monitor directory] InsrumentedProgram MonitorLibrary
    java -cp [rvmonitorrt]:[monitor directory] TestWithMain

This is why we have asked you to add rv-monitor/lib/rt.jar:. to your CLASSPATH.
It makes calling javac and java from this directory (/HasNext) simpler.

    javac InstrumentedProgram MonitorLibrary
    java TestWithMain

______________

To run our examples from this directory,

1)
    rv-monitor rvm/HasNext.rvm
    javac HasNext_1/HasNext_1.java rvm/HasNextRuntimeMonitor.java
    java HasNext_1.HasNext_1

2)
    javac HasNext_2/HasNext_2.java rvm/HasNextRuntimeMonitor.java
    java HasNext_2.HasNext_2

